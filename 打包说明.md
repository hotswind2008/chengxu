# 三门问题游戏 - Windows EXE 打包说明

## 方法一：在Windows电脑上打包（推荐）

### 步骤：

1. **将以下文件复制到Windows电脑**
   - `monty_hall_final.py` （游戏主程序）
   - `requirements.txt` （依赖包列表）
   - `build_exe.py` （打包脚本）

2. **在Windows上安装Python**
   - 下载：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

3. **打开命令提示符（CMD）**
   - 按 `Win + R`，输入 `cmd`，回车

4. **切换到文件所在目录**
   ```cmd
   cd C:\path\to\your\files
   ```

5. **安装依赖**
   ```cmd
   pip install -r requirements.txt
   ```

6. **运行打包脚本**
   ```cmd
   python build_exe.py
   ```

7. **完成！**
   - 可执行文件位置：`dist/三门问题游戏.exe`
   - 直接双击即可运行

---

## 方法二：手动打包（如果自动打包失败）

### 在Windows命令提示符中执行：

```cmd
# 1. 安装 PyInstaller
pip install pyinstaller

# 2. 打包命令
pyinstaller --name=三门问题游戏 --onefile --windowed --clean monty_hall_final.py
```

### 参数说明：
- `--name=三门问题游戏`：设置exe文件名
- `--onefile`：打包成单个exe文件
- `--windowed`：运行时不显示控制台窗口
- `--clean`：清理临时文件

---

## 方法三：使用在线服务（无需Windows电脑）

### 使用 GitHub Actions 自动打包：

1. 将代码上传到 GitHub
2. 创建 `.github/workflows/build.yml` 文件
3. GitHub Actions 会自动打包并提供下载

**build.yml 内容：**
```yaml
name: Build Windows EXE

on: [push]

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
    
    - name: Build EXE
      run: |
        pyinstaller --name=三门问题游戏 --onefile --windowed --clean monty_hall_final.py
    
    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: MontyHall-Game-Windows
        path: dist/三门问题游戏.exe
```

---

## 常见问题

### Q1: 打包后的exe文件很大（>100MB）？
**A:** 这是正常的，因为包含了Python解释器和所有依赖库。

### Q2: 运行exe时显示"正在提取文件"？
**A:** 首次运行时PyInstaller会解压缩，稍等几秒即可。

### Q3: Windows Defender 报警？
**A:** 这是误报，因为exe是新生成的。可以添加到信任列表。

### Q4: emoji显示为方块？
**A:** 需要Windows 10或更高版本，确保系统支持emoji。

### Q5: 打包失败，提示缺少模块？
**A:** 运行 `pip install -r requirements.txt` 安装所有依赖。

---

## 文件大小优化（可选）

如果想减小exe文件大小：

```cmd
# 使用 --exclude-module 排除不需要的模块
pyinstaller --name=三门问题游戏 ^
    --onefile ^
    --windowed ^
    --exclude-module _tkinter ^
    --exclude-module numpy ^
    --clean ^
    monty_hall_final.py
```

---

## 添加图标（可选）

如果有 `.ico` 图标文件：

```cmd
pyinstaller --name=三门问题游戏 ^
    --onefile ^
    --windowed ^
    --icon=icon.ico ^
    --clean ^
    monty_hall_final.py
```

---

## 技术支持

如果遇到问题：
1. 确保Python版本 >= 3.8
2. 确保PyInstaller版本 >= 5.0
3. 检查是否安装了所有依赖包
4. 尝试以管理员身份运行命令提示符

---

## 分发给其他用户

打包完成后，只需要分发 `dist/三门问题游戏.exe` 这一个文件即可！
用户无需安装Python即可直接运行。

**系统要求：**
- Windows 10 或更高版本
- 64位操作系统
- 无需安装任何额外软件

---

祝你打包顺利！🎮





